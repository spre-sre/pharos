"""Extension packages for Lumino.

Each sub-package exposes an EXTENSION object satisfying the Extension protocol
defined in core.extension.  This package is intentionally thin — discovery
and activation are handled by core.extension.activate_extensions().
"""
